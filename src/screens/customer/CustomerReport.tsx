import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const CustomerReport = () => {
  return (
    <View>
      <Text>CustomerReport</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default React.memo(CustomerReport);
