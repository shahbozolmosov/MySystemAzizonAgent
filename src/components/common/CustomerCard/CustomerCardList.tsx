import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CustomerCardList = () => {
  return (
    <View style={styles.list}>
      <Text>CustomerCardList</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  list: {},
});

export default CustomerCardList;
